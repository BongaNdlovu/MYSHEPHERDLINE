appId: ${E2E_APP_ID}
---
- runFlow: ../subflows/login.yaml
- tapOn:
    id: tab-tasks
- assertVisible:
    id: tasks-screen
# Seed data guarantees at least one open task for the test shepherd.
- tapOn:
    text: ${E2E_TASK_TITLE}
- tapOn:
    id: "task-toggle-.*"
